# AI 自動化求職 Pipeline_展示用 (MVP)

多平台**職缺爬蟲** → 去重合併 → **規則 + AI 評分** → 抓取 JD → **AI 生成 CV / 面試 QA** → 寫入 **Notion** 追蹤。
一條龍把找工作流程自動化。本頁為**靜態前端展示**，職缺、公司、薪資與履歷內容皆為**虛構範例**。

> 📌 公開作品展示頁；不含任何金鑰、Notion token、真實個人履歷或個資。

## 線上展示

直接以瀏覽器開啟 `index.html`；若資源載入異常：

```bash
python -m http.server 8000   # 開 http://localhost:8000
```

## 功能特色

- **Pipeline 流程動畫**：爬蟲 → 去重合併 → 評分 → 抓 JD → AI 生成 → Notion，依序亮燈
- **職缺看板**：依 AI 評分 A / B / C 分組摺疊，點職缺看匹配理由
- **AI 生成**：選一個職缺，逐字生成「客製 CV 重點」或「面試 QA」（模擬）
- 深色專業介面、響應式

## 技術關鍵字

`多平台爬蟲 (104 / 1111)` · `Web Scraping` · `反爬處理` · `規則 + AI 評分` · `JD 解析` ·
`AI 生成 (CV / 面試 QA)` · `LLM` · `Notion API` · `去重同步` · `Pipeline 設計` · `Python`

## 在這個專案我實際做了什麼（高層說明）

- 🕷️ **多來源爬蟲 pipeline**：104 / 1111 等多平台，處理 Cloudflare cookie、參數差異與分頁，輸出統一格式
- ⭐ **規則 + AI 雙層評分**：純規則秒級評分（無需 API）＋ AI 對高分職缺加值，分 A / B / C
- 📄 **JD 解析**：自動補抓職缺 JD 作為生成依據
- 🤖 **AI 生成 CV / 面試 QA**：依 JD 用 LLM 產出客製履歷重點與面試問答
- 🗂️ **Notion API 自動化**：寫入資料庫並去重，建立可追蹤求職看板
- 🏗️ **可擴充架構**：主流程一鍵跑全平台，模組化方便新增爬蟲與步驟

> 實際爬蟲細節、評分規則、生成 prompt 與個人履歷資料屬隱私 / know-how，未公開於此展示，可於面談時說明。

## 作者

Jason ｜ jason0308p@gmail.com

---
*本頁為展示用途，職缺與資料為虛構範例，僅供參考。*
